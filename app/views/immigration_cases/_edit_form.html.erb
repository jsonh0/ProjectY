<%= form_with(model: immigration_case) do |form| %>
  <% if immigration_case.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(immigration_case.errors.count, "error") %> prohibited this immigration_case from being saved:</h2>

      <ul>
        <% immigration_case.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
     <%= form.hidden_field :foreign_nationals_id, value: immigration_case.foreign_nationals_id %>
     <h5><%=immigration_case.foreign_nationals_id%></h5>
  </div>

  <div>
    <%= form.label :case_type, style: "display: block" %>
<%= form.collection_select :case_type, ImmigrationCase.case_types.keys, :to_s, :humanize, { include_blank: "Select a Case Type" } %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
<%= form.collection_select :status, ImmigrationCase.statuses.keys, :to_s, :humanize, { include_blank: "Select a Status" } %>
  </div>

  <div>
    <%= form.label :receipt_number, style: "display: block" %>
    <%= form.text_field :receipt_number %>
  </div>

  <div>
    <%= form.label :received_date, style: "display: block" %>
    <%= form.date_field :received_date %>
  </div>
 <div>
    <%= form.label :sent_date, style: "display: block" %>
    <%= form.date_field :sent_date %>
  </div>

  <div>
    <%= form.label :expiration_date, style: "display: block" %>
    <%= form.date_field :expiration_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
