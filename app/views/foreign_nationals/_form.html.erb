<%= form_with(model: foreign_national) do |form| %>
  <% if foreign_national.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(foreign_national.errors.count, "error") %> prohibited this foreign_national from being saved:</h2>

      <ul>
        <% foreign_national.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>

  <%= form.label :status, "Select status" %>
  <%= form.collection_select :status, ForeignNational.statuses.keys, :to_s, :humanize, { include_blank: "Select a Status" } %>

  </div>

  <div>
    <%= form.label :birthday, style: "display: block" %>
    <%= form.date_field :birthday %>
  </div>

  <div>
    <%= form.label :address, style: "display: block" %>
    <%= form.text_field :address %>
  </div>

  <div>
      <%= form.hidden_field :account_id, value: @account_id %>
  </div>
  <div>
    <h3><%=@account_id%></h3>
  </div>

  <div>
    <%= form.submit %>
  </div>
  
<% end %>
